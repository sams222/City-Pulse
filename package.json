{
  "name": "city-pulse",
  "private": true,
  "type": "module",
  "scripts": {
    "seed:firestore": "node scripts/seed-firestore.mjs",
    "start": "npm --prefix mobile run start",
    "web": "npm --prefix mobile run web",
    "android": "npm --prefix mobile run android",
    "export:web": "npm --prefix mobile run export:web",
    "deploy:web": "npm run export:web && npx firebase deploy --only hosting:city-pulse"
  },
  "devDependencies": {
    "dotenv": "^17.3.1",
    "firebase-admin": "^13.0.0",
    "firebase-tools": "^14.0.0"
  }
}
